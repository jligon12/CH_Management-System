<a href="/customer_add">+ Add Customer</a> <a href="/customer_update">Update Customer</a>
<table id="customer-table">
    <thead>
        <tr>
            {{#each data.[0]}}
            <th>
                {{@key}}
            </th>
            {{/each}}
            <th>
            </th>
        </tr>
    </thead>

    <tbody>
        {{#each data}}
        <tr data-value={{this.customerID}}>
            <td>{{this.customerID}}</td>
            <td>{{this.name}}</td>
            <td>{{this.email}}</td>
            <td>{{this.phoneNum}}</td>
            {{!-- pass this.customerID to the update form  --}}
            {{!-- update form is rendered by clicking this button --}}
            {{!-- <td><button onclick="updateCustomer({{this.customerID}})">Update</button></td> --}}
        </tr>
        {{/each}}
    </tbody>
</table>